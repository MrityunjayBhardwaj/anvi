---
description: Debug a failure using Anvi's diagnose chain (gather -> classify -> scan -> compress -> prove -> fix)
argument-hint: <symptom or failing test>
agent: agent
tools: [edit, search, runCommands, problems]
---

Read `~/.claude/anvi/copilot-compat/debugger-hook.md` and follow the
diagnose chain instead of a hypothesis-and-retry loop while investigating:

${input:symptom}
